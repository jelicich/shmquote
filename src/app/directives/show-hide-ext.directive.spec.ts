import { ShowHideExtDirective } from './show-hide-ext.directive';

describe('ShowHideExtDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowHideExtDirective();
    expect(directive).toBeTruthy();
  });
});
