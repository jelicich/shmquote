import { LayoutExtDirective } from './layout-ext.directive';

describe('LayoutExtDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutExtDirective();
    expect(directive).toBeTruthy();
  });
});
